import { styled } from 'styled-components';

export const Form = styled.form`
  width: 33%;
  max-width: 450px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  align-items: start;
`;
