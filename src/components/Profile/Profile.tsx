import React from 'react';
import Menu from '../Menu/Menu';
import { Wrapper } from './styled';

const Profile = () => {
  return (
    <Wrapper>
      <Menu />
    </Wrapper>
  );
};

export default Profile;
